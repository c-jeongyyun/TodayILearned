## 오늘 배운 것 
1. css로 포인터 이벤트 컨트롤☝️  
   `pointer-events` 속성으로 포인터 이벤트를 조절할 수 있다. 
   핵심 속성은 아래와 같다. 
     
   ```css
   pointer-events: auto ; // 일반적인 포인터 이벤트와 동일하게 작동한다. 
   pointer-events: none ; // 포인터 이벤트 작동을 막는다.
   ```
   참고: https://developer.mozilla.org/ko/docs/Web/CSS/pointer-events
