## Form 엔터 이벤트로 제출하기

form 으로 하는 경우, input에 focus 갈 때만 엔터 이벤트가 유효하다. 

따라서 input에 focus없이 엔터 이벤트를 달고 싶으면, 전역적으로 keydown 이벤트를 넣어야 한다. 

그런데 이때, 기존에 form 에 달려있는 엔터 이벤트(input 포커스일 때 적용되는 엔터이벤트) 를 남겨두게 되면 요청이 중복으로 가게된다. 이것 주의!